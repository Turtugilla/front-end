import { NombreProhibidoDirective } from './nombre-prohibido.directive';

describe('NombreProhibidoDirective', () => {
  it('should create an instance', () => {
    const directive = new NombreProhibidoDirective();
    expect(directive).toBeTruthy();
  });
});
